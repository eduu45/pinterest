import Nav from '@/app/Components/Nav';
import Body from "@/app/Components/Body";

export default function home () {
  return(
      <main className="flex">
        <Nav/>
        <Body/>
      </main>
  )
}